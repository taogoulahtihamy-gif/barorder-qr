You are not modifying the real files.

Git still says:
"nothing to commit, working tree clean"

Now directly edit the actual project files.

TASK 1 — Fix invisible demo accounts text

Open the real login page component:
- client/src/pages/admin/LoginPage.jsx
OR
- client/src/pages/LoginPage.jsx

Find:
text-white/20

Replace ALL occurrences with:
text-white/60

Also ensure:
- demo accounts container uses readable opacity
- no black text on black background
- spacing between demo account lines is clean

TASK 2 — Fix production socket URL

Search the entire repository for:
localhost:5000

Replace ALL production socket/API localhost references with:

https://barorder-qr.onrender.com

IMPORTANT:
- socket.io must never connect to localhost in production
- use VITE_API_URL or the Render URL
- frontend deployed on Vercel must connect to Render backend

TASK 3 — Verify changes are REAL

After editing:
run:
git status

It MUST show modified files.

Then run:
git add .
git commit -m "Fix login visibility and production socket URL"
git push origin main

FINAL REQUIREMENT:
Do not say “fixed” unless git status actually showed modified files and commit/push succeeded.