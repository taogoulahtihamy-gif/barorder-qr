URGENT FIX customer black screen after order creation.

Problem:
After a customer creates an order from QR/client checkout, the client side becomes black/blank.
The order may be created, but the confirmation/tracking page crashes.

Do NOT modify admin.
Do NOT modify backend unless strictly necessary.
Do NOT break order creation, checkout, cart, menu, QR, multi-restaurant.

Fix only customer post-order flow.

1. Inspect customer flow after confirm order:
- CheckoutPage
- OrderConfirmationPage
- OrderTrackingPage
- CustomerLayout
- router/index.jsx
- orderService
- cart store

2. Find the crash source:
Search for:
- undefined order
- order.items.map without fallback
- order.customer.name
- order.table.number
- t(...) without useT()
- tStatus(...) without useT()
- navigate to wrong route
- invalid orderId
- missing restaurant slug/tableId
- Response/null handling
- localStorage parse error

3. Add safe guards:
- if order is loading: show premium loader
- if order not found: show clean error with button “Retour au menu”
- if order.items missing: use []
- if customer missing: use fallback
- if table missing: use tableId from route
- if total missing: calculate from items or show 0

4. Routing after order creation:
After successful checkout, navigate to:
 /r/:slug/table/:tableId/order/:orderId

Do NOT navigate to a route that does not exist.
Keep slug and tableId.

5. Customer order tracking page:
Must render safely even if backend response is partial.

Display:
- order number
- status
- table
- items
- total
- payment status
- estimated time
- button “Retour au menu”

6. Error boundary:
Add CustomerErrorBoundary around customer routes.
If a customer page crashes:
- show clean message instead of black screen
- button to return to menu
- no blank page

7. Bilingual:
All new labels use i18n.
No hardcoded visible text if possible.

8. Build and push:
npm run build
git add .
git commit -m "Fix customer black screen after order creation"
git push origin main

Acceptance:
- create order from QR
- no black screen
- confirmation/tracking page opens
- order details visible
- menu/cart still work
- admin still receives order