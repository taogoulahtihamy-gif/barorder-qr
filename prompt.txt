URGENT FIX: BarOrder realtime synchronization is broken after last big update.

Problems:
1. Customer orders no longer appear in admin orders/kitchen.
2. Server calls accept/resolve no longer sync.
3. Kitchen status changes are not reliable.
4. Browser shows:
TypeError: Response constructor: Response body is given with a null body status

Priority:
Restore synchronization and working order/server-call flow.
Do not add new features.

A. Fix PWA/service worker/fetch error
Search for:
new Response(
status: 204
status: 205
null
service-worker
sw.js
offline fallback

Fix:
- Never create Response(null, { status: 204/205 }) if browser rejects it.
- For 204/205, use new Response("", { status: 200 }) or return original response.
- If service worker causes issues, disable/unregister it temporarily.
- PWA must not intercept API calls:
  /api/*
  socket.io/*
  onrender.com/*
  vercel.app/api/*

B. Restore API synchronization
Check all frontend services:
- orderService
- serverCallService
- socketService
- notificationService

Ensure production API base is:
https://barorder-qr.onrender.com

No localhost.
No broken Vercel /api proxy.

C. Restore order creation flow
POST /api/public/orders must:
- create order
- create order_items
- return created order
- emit new_order, orders_updated, kitchen_updated

Admin pages must:
- listen to socket events
- refetch every 5 seconds fallback

D. Restore server calls
When client clicks “Appeler un serveur”:
- create server call
- show in /admin/server-calls
- waiter/admin receive update
- accept/resolve must update status immediately
- emit server_call_created/server_call_updated

E. Restore kitchen workflow
- Commencer => preparing
- Prête => ready
- local state updates immediately
- refetch fallback
- no print unless clicking Imprimer

F. Remove dangerous auto print
Disable all automatic print triggers by default.
Print only when clicking explicit Imprimer button.

G. Build and push
Run:
npm run build
git status
git add .
git commit -m "Restore realtime sync and fix PWA response error"
git push origin main

Acceptance tests:
1. Client creates order -> appears in /admin/orders within 5 seconds.
2. Same order appears in /admin/kitchen within 5 seconds.
3. Kitchen Commencer moves to En préparation.
4. Kitchen Prête moves to Prête.
5. Appeler serveur appears in admin/server calls.
6. Accept/resolve server call works.
7. No TypeError Response constructor error.
8. No automatic print window.