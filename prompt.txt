Do not explain the steps anymore.

Directly edit and apply the responsive fixes to the actual project files now.

Modify the existing files in the workspace instead of showing code examples.

Fix the remaining mobile responsive and QR routing issues in BarOrder QR.

Problems:
1. On mobile admin pages (/admin/orders and /admin/payments), the filter chips overlap each other:
   - "Tout", "Wave", "Cash", "Orange Money", "Payé", "En attente"
   - "Tout", "En attente", "Acceptée", "En préparation", etc.
   They must NOT overlap.

Required fix for filters:
- Create a reusable horizontal scroll container for filter chips.
- The container must use:
  overflow-x: auto;
  overflow-y: hidden;
  display: flex;
  flex-wrap: nowrap;
  gap: 10px;
  width: 100%;
  max-width: 100%;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
- Each chip/button must use:
  flex: 0 0 auto;
  white-space: nowrap;
  min-height: 44px;
  padding: 0 18px;
- Hide scrollbar visually.
- The page/body must still have no horizontal scroll. Only the chip row can scroll inside itself.
- Apply this to OrdersPage, PaymentsPage, KitchenPage if it has status filters, and any admin page with filter chips.

2. QR/table entry route issue:
When scanning or opening:
  /r/le-palais/table/1
the app must show the table welcome page like:
  Le Palais
  Bienvenue à la Table 1
  Reprendre ma commande
  Voir le menu
  Parcourir le menu
  Appeler un serveur

It must NOT redirect directly to:
  /r/le-palais/menu/1

Required fix:
- Find the QR generation code.
- Ensure generated QR URL is:
  `${PUBLIC_APP_URL}/r/${restaurantSlug}/table/${tableNumberOrId}`
NOT:
  `${PUBLIC_APP_URL}/r/${restaurantSlug}/menu/${tableNumberOrId}`

- Find React routes.
- Ensure /r/:slug/table/:tableId renders the TableWelcomePage / TableLandingPage.
- Ensure the “Voir le menu” button is the only action that navigates to /r/:slug/menu/:tableId.
- Do not auto-redirect from table page to menu page.
- Remove any useEffect or navigate() that sends /table/:tableId directly to /menu/:tableId.

3. Preserve current working backend/API.
4. Do not change database logic.
5. Do not change payment/order creation logic.
6. Keep premium dark UI.
7. Run npm run build and fix errors.

Acceptance tests:
- On iPhone 390px, /admin/orders filter chips do not overlap.
- On iPhone 390px, /admin/payments filter chips do not overlap.
- Body has no horizontal scroll.
- Filter row scrolls horizontally inside its own container.
- /r/le-palais/table/1 opens welcome page.
- Clicking “Voir le menu” opens /r/le-palais/menu/1.


Disable unwanted browser zoom and make all pages fully adaptive/dynamic on mobile.

Requirements:
- Add proper viewport meta tag:
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover" />

- Prevent accidental zoom on iPhone/Safari.
- Inputs/selects/buttons must use minimum font-size: 16px to avoid iOS auto zoom.
- The UI must adapt fluidly to all mobile sizes without needing pinch zoom.
- No content should require zooming to read.
- Use responsive typography with clamp() where necessary.
- Images/cards/buttons/text must scale dynamically.
- Respect safe areas on iPhone (notch + bottom bar).
- No horizontal overflow anywhere.
- The app must feel like a native mobile app.

Keep the premium dark design and existing functionality unchanged.

