URGENT FIX QR scan customer welcome error.

Current state:
- Header correctly shows Le Palais and logo.
- But when scanning QR route /r/le-palais/table/:tableId, page shows:
“Une erreur est survenue”
- So restaurant loading works, but customer table welcome content crashes.

Fix only customer QR table welcome page.

Do NOT touch:
- admin
- restaurants CRUD
- orders
- kitchen
- users
- roles

Tasks:

1. Inspect customer table route:
- /r/:slug/table/:tableId
- TableWelcome
- CustomerWelcome
- CustomerLayout
- restaurant loading hook
- table loading hook
- server call button

2. Find crash source:
Search for unsafe access:
- table.id
- table.number
- restaurant.logo_url
- restaurant.primary_color
- restaurant.name
- t(...)
- tStatus(...)
- useParams tableId
- Number(tableId)
- tableId.map or undefined.map

3. Add safe fallbacks:
- if restaurant exists but table is not fetched, use tableId from route
- show “Table 3” from params
- do not crash if table object is null
- do not require table database record to render welcome page

4. Correct table display:
Never display literal “:tableId”.
Always display:
FR: Table 3
EN: Table 3

5. Button routing:
Voir le menu must navigate to:
 /r/:slug/table/:tableId/menu

Appeler un serveur must use same slug/tableId.

6. If table does not exist in database:
show clean message:
“Cette table n’existe pas”
But do not crash.

7. Add console error detail in development only:
console.error("[CUSTOMER WELCOME ERROR]", error)

8. Build and push:
npm run build
git add .
git commit -m "Fix QR table welcome crash"
git push origin main

Acceptance:
- scanning QR opens welcome page
- Le Palais logo/name visible
- Table number visible
- no error screen
- Voir le menu works
- Appeler serveur works