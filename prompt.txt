Upgrade BarOrder order workflow to a real restaurant realtime system.

Implement full order lifecycle.

Statuses:
- pending
- preparing
- ready
- served
- paid
- cancelled

Tasks:

1. Backend
- Add status validation
- Add timestamps:
  created_at
  preparing_at
  ready_at
  served_at
  paid_at
- Add PATCH /api/orders/:id/status

2. Role permissions
- waiter:
  create orders
  mark served
- kitchen:
  mark preparing
  mark ready
- cashier:
  mark paid
- admin/super_admin:
  full access

3. Frontend realtime
- Orders page auto-refresh every 5 seconds
OR websocket realtime if already configured

4. UI
- Color badges per status
- Sound notification for new orders
- Kitchen page only shows:
  pending + preparing
- Waiter page shows:
  ready + served
- Cashier page shows:
  served + paid

5. Add order timeline UI
Example:
Pending -> Preparing -> Ready -> Served -> Paid

6. Build and push.

Acceptance:
- Full restaurant workflow works
- Roles only see allowed actions
- Orders update live
- UI remains premium dark gold