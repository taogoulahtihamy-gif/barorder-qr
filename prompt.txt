URGENT FIX admin login premium inputs.

Current problem:
- email is prefilled with administrateur1@barorder.com
- inputs become pale yellow/white when typing
- floating labels overlap the input border
- password eye is almost invisible
- UI looks broken

Fix only /admin/login.

Rules:
1. Email and password must be empty by default.
2. Remove any hardcoded defaultValue/value:
   administrateur1@barorder.com
   admin@barorder.sn
   admin123
   or any demo credential.
3. Disable browser autofill yellow background visually.
4. Inputs must stay dark premium even when focused, typed, or autofilled.

CSS required:
- input background remains #0f0f12 / dark
- text white
- border subtle
- focus border gold
- autofill override:
  input:-webkit-autofill {
    -webkit-box-shadow: 0 0 0 1000px #0f0f12 inset !important;
    -webkit-text-fill-color: #ffffff !important;
  }

5. Remove broken floating labels OR make them clean.
Preferred:
- use simple labels above inputs
- no overlapping labels.

6. Eye icon:
- visible grey/white
- centered vertically
- clickable
- does not submit form

7. Keep design premium:
- dark glass card
- gold button
- restaurant name
- logo/monogram
- responsive mobile

8. Do not change auth logic.
9. Build and push.

Acceptance:
- login opens with empty email/password
- fields stay dark when typing
- no yellow input background
- labels aligned cleanly
- eye icon visible
- login still works