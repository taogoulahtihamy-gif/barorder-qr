Fix remaining BarOrder QR stabilization points before deployment.

Scope: only fix points 1 and 2:
1. Mobile responsive UI
2. Code cleanup
Do NOT touch deployment, backend database schema, payment/order logic, or working API routes.

Current issue still visible on customer menu mobile:
On /r/le-palais/menu/1, the category chips row:
"Tout", "Plats", "Vin", "Grillades", "Boissons", "Desserts", "Cocktails"
is still overlapping/cut on iPhone.
It must become a clean horizontal scroll row.

Required fix for ALL chip/filter rows, client and admin:
- Create/ensure one reusable CSS class:
  .filter-scroll or .chip-scroll
- Apply it to:
  - client menu categories
  - admin orders filters
  - admin payments filters
  - any other status/category chip row

CSS requirements:
- display: flex;
- flex-wrap: nowrap;
- gap: 10px;
- width: 100%;
- max-width: 100%;
- overflow-x: auto;
- overflow-y: hidden;
- -webkit-overflow-scrolling: touch;
- scrollbar-width: none;
- padding-bottom: 4px;
- min-width: 0;

Hide scrollbar:
- .filter-scroll::-webkit-scrollbar { display: none; }

Every chip/button inside must have:
- flex: 0 0 auto;
- white-space: nowrap;
- min-height: 44px;
- padding: 0 18px;
- max-width: none;
- border-radius: 999px;

Important:
- Only the chip row may scroll horizontally.
- The whole page/body must NOT scroll horizontally.
- No chip should overlap another chip.
- No chip should be cut in half unless user scrolls the chip row.

Global mobile responsive cleanup:
- html, body, #root:
  width: 100%;
  max-width: 100%;
  overflow-x: hidden;
- all flex/grid children that contain text:
  min-width: 0;
- cards:
  width: 100%;
  max-width: 100%;
  overflow: hidden;
- long text/order numbers:
  overflow-wrap: anywhere or truncate where appropriate.
- inputs/buttons/selects:
  font-size: 16px minimum on mobile to prevent iPhone zoom.
- viewport meta must prevent accidental zoom and respect safe area:
  width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover

Client menu page specific:
- Search bar width must be 100%.
- Product cards must not overflow.
- Category chips must scroll horizontally inside their own row.
- Add enough bottom padding so Safari bottom bar does not cover last products/cart button.

Admin dashboard/order card cleanup:
- Status badges must stay inside cards.
- Amounts must always show FCFA.
- Raw ISO dates must be formatted as dd/mm/yyyy hh:mm.
- Cards must not overflow.

Code cleanup:
- Remove useless console.log/debug logs.
- Remove obsolete mock fallback warnings in production.
- Remove dead comments and unused imports.
- Keep only necessary error logging.
- Do not remove working features.
- Do not change API endpoints that currently work.
- Do not change database schema.

Files likely involved:
- client/src/styles/responsive.css
- client/src/main.jsx
- client/src/pages/MenuPage.jsx or customer menu page equivalent
- client/src/pages/admin/OrdersPage.jsx
- client/src/pages/admin/PaymentsPage.jsx
- client/src/pages/admin/DashboardPage.jsx
- shared chip/filter components if they exist

Acceptance tests:
- iPhone 390px /r/le-palais/menu/1:
  category chips do not overlap.
  category row scrolls horizontally.
  page itself has no horizontal scroll.
- iPhone 390px /admin/orders:
  filter chips do not overlap.
- iPhone 390px /admin/payments:
  filter chips do not overlap.
- Dashboard latest orders:
  badges stay inside cards.
  amounts show FCFA.
  dates are readable.
- npm run build passes.

Apply changes directly to project files, not only explanations.