Final cleanup before production validation.

1. Stop tracking client/dist:
git rm -r --cached client/dist

2. Ensure .gitignore contains:
dist
client/dist
node_modules
.env
client/.env
server/.env

3. Do not delete local files, only untrack generated files.

4. Clean client install:
cd client
rm -rf node_modules
npm install
npm run build

5. Commit:
git add .
git commit -m "Final cleanup generated files and build config"
git push origin main

Acceptance:
- client/dist is not tracked anymore
- .env files are not tracked
- npm run build passes
- git status clean