Clean and stabilize BarOrder QR technically without adding new features.

Context:
Project is already advanced. Do not rebuild everything.
Goal is to clean, stabilize, and prepare production.

Do NOT break:
- admin login
- roles/users
- restaurants
- QR table flow
- client menu/cart/checkout
- orders
- kitchen
- payments simulated
- promotions
- tables
- statistics
- socket sync

==================================================
1. CLEAN CUSTOMER ROUTES
==================================================

Audit router/index.jsx and customer routes.

Problem:
There are old/duplicate routes like:
- /r/:slug/table/:tableId
- /r/:restaurantSlug/table/:tableId
- /menu/:restaurantSlug/:tableId
- /table/:tableId

Keep canonical routes:

Public restaurant:
- /r/:slug
- /r/:slug/menu

QR table:
- /r/:slug/table/:tableId
- /r/:slug/table/:tableId/menu
- /r/:slug/table/:tableId/cart
- /r/:slug/table/:tableId/checkout
- /r/:slug/table/:tableId/order/:orderId
- /r/:slug/table/:tableId/assistance

Admin:
- /admin/*
- /admin/login

Rules:
- no automatic redirect from QR table to wrong route
- preserve slug and tableId everywhere
- no literal “:tableId” visible
- old routes can redirect safely to canonical routes

==================================================
2. MULTI-RESTAURANT ISOLATION CHECK
==================================================

Verify all data queries are scoped by restaurant_id.

Tables requiring restaurant_id:
- users
- products
- categories
- tables
- orders
- order_items if useful
- payments
- promotions
- server_calls

Rules:
- super_admin can see all or selected restaurant
- admin/manager/waiter/kitchen/cashier see only their restaurant
- public client routes load restaurant by slug
- no data leakage between restaurants

If restaurant_id is null on old data:
- assign it to Le Palais safely
- never delete data

==================================================
3. API BASE CLEANUP
==================================================

Search entire client/src for:
- localhost
- 127.0.0.1
- hardcoded Render URLs
- hardcoded Vercel URLs

Use central API config only.

Production API:
- use VITE_API_URL

Development:
- fallback to localhost only in dev mode

No production code should call localhost.

==================================================
4. REMOVE DEBUG NOISE
==================================================

Remove or guard debug logs:
- [REFETCH TRIGGERED]
- [SOCKET EVENT RECEIVED]
- [CUSTOMER PARAMS]
- [CUSTOMER RESTAURANT]
- excessive console.log

Keep console.error for real errors.
Debug logs only in development.

==================================================
5. ENV SECURITY
==================================================

Check .gitignore:
Must ignore:
- .env
- client/.env
- server/.env
- node_modules
- dist
- build

Keep:
- .env.example

Do not commit real secrets.

If real .env exists in repo tracking:
- stop tracking it safely with git rm --cached
- do not delete local file

==================================================
6. BUILD STABILITY
==================================================

Do not rely on broken local node_modules.

Clean install guidance:
- if build fails because Rollup optional dependency missing, fix package lock/install cleanly
- do not commit node_modules

Run:
cd client && npm install && npm run build
cd server && npm install && npm run start-check if available

If no start-check exists, at least run:
node --check src/server.js

==================================================
7. ERROR BOUNDARIES
==================================================

Ensure customer and admin routes have error boundaries:
- no black screen
- clean fallback page
- button back to menu/admin dashboard

Do not hide real errors silently.
Show readable user message.

==================================================
8. RESPONSIVE QUICK CHECK
==================================================

Do not redesign.
Only fix obvious layout regressions:
- no horizontal overflow
- bottom nav does not cover content
- modal buttons visible
- table cards not clipped

==================================================
9. ACCEPTANCE TESTS
==================================================

Verify:

Customer QR:
- /r/le-palais/table/3 opens correctly
- logo/name visible
- Table 3 visible
- Voir le menu opens correct menu
- cart/checkout/order tracking work

Admin:
- login works
- restaurants page works
- users page works
- orders page works
- kitchen works
- tables QR works
- statistics/dashboard work

Multi-restaurant:
- create second restaurant
- admin scoped data remains isolated
- Le Palais still works

Console:
- no critical ReferenceError
- no black screen
- no timeout 3000ms remaining

==================================================
10. COMMIT
==================================================

Run:
git status
git add .
git commit -m "Clean routes and stabilize multi restaurant foundation"
git push origin main

Final response must include:
- files changed
- what was cleaned
- build result
- any remaining risk