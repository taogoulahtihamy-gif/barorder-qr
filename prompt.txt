Fix mobile layout for OrdersPage filter/header section.

Problem:
On mobile, the “Auto-print cuisine” checkbox overlaps the status filters and breaks the layout.

Required fixes:

1. Mobile responsive header
On screens < 768px:
- place filters on first row
- place “Auto-print cuisine” on second row
- full width
- aligned left
- no overlap
- allow horizontal scroll for filter pills if needed

2. Desktop behavior
On desktop:
- keep current inline layout
- filters + auto-print aligned correctly

3. Styling
- cleaner spacing
- reduce checkbox text size slightly on mobile
- add margin-top between rows
- preserve dark premium design

4. Technical
Use:
- flex-wrap
- gap
- overflow-x-auto
- min-width for filter pills
- responsive Tailwind classes

5. Acceptance
- no overlap on iPhone/mobile
- filters remain clickable
- checkbox visible and aligned
- no horizontal broken UI
- build passes
- commit and push