EMERGENCY FIX FOR RENDER CRASH.

The backend crashes because:

src/routes/users.js imports:
updateUser,
deleteUser

from:
../controllers/userController.js

but userController.js does not export updateUser/deleteUser.

Fix immediately.

Open:
src/routes/users.js

Do this:
1. Remove updateUser and deleteUser from the import.
2. Remove or comment any routes using updateUser/deleteUser.
3. Keep only getUsers and createUser routes.
4. Do not touch working routes: public restaurants, menu, orders, payments, kitchen, server-calls.
5. Run/start backend and make sure there is no crash.
6. Run build if needed.
7. Commit the fix.

Acceptance:
- backend starts without "does not provide an export named updateUser"
- Render deploy succeeds
- /api/public/restaurants returns 200
- admin login page still opens