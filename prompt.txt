Optimize BarOrder realtime synchronization speed.

Current situation:
Synchronization works again, but it takes 10–20 seconds.
We need near-instant updates under 1–2 seconds.

Goal:
Use socket.io realtime first, polling only as fallback.

Fix:

1. Backend socket emit
After each action, emit immediately:
- order created => new_order + orders_updated + kitchen_updated
- order status updated => order_status_updated + orders_updated + kitchen_updated
- server call created => server_call_created + server_calls_updated
- server call accepted/resolved => server_call_updated + server_calls_updated
- payment updated => payment_updated + orders_updated

2. Frontend listeners
Ensure admin pages listen to these events:
- /admin/orders
- /admin/kitchen
- /admin/server-calls
- /admin/payments
- dashboard

On socket event:
- refetch immediately
- do not wait for polling interval

3. Socket connection
Fix production socket URL:
https://barorder-qr.onrender.com

No localhost.
No duplicate socket connections.
No memory leaks.

4. Polling fallback
Keep polling, but reduce to:
- 5 seconds maximum
Only fallback if socket disconnected.

5. Add logs:
Frontend:
[SOCKET CONNECTED]
[SOCKET EVENT RECEIVED] eventName
[REFETCH TRIGGERED]

Backend:
[SOCKET EMIT] eventName

6. Acceptance:
- New client order appears in admin orders in under 2 seconds.
- New order appears in kitchen in under 2 seconds.
- Server call appears in waiter/admin in under 2 seconds.
- Status change updates all open tabs in under 2 seconds.
- If socket fails, polling still updates within 5 seconds.

Build, commit, push.