Production still connects to localhost:5000.

Search the ENTIRE repository for ALL occurrences of:

localhost:5000
socket.io
io(
new WebSocket(
VITE_SOCKET_URL
VITE_API_URL

Fix ALL production socket connections.

IMPORTANT:
The deployed Vercel frontend MUST connect ONLY to:
https://barorder-qr.onrender.com

Tasks:

1. Remove EVERY hardcoded localhost socket/API URL.
2. Fix socket connection in all files:
- socket.js
- socketService.js
- websocket.js
- api.js
- axios.js
- hooks
- services
- admin pages
- realtime listeners

3. Production logic:
- if production:
  use https://barorder-qr.onrender.com
- if local dev:
  use localhost only in development mode

4. Use:
const API_URL = import.meta.env.VITE_API_URL || "https://barorder-qr.onrender.com"

5. Use the same URL for socket.io.

6. Rebuild frontend completely.

7. Delete old dist/build cache if needed.

8. Run:
npm run build

9. Commit real changes:
git add .
git commit -m "Fix production socket URL"
git push origin main

10. Confirm with:
git grep "localhost:5000"

Expected result:
NO localhost:5000 remaining in production frontend code.