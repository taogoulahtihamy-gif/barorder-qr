URGENT FIX ONLY OrdersPage black screen.

Problem:
- /admin/role-test works now.
- /admin/orders is still black.
- Do not touch role-test.
- Do not touch other pages unless needed.
- Fix OrdersPage only.

Tasks:
1. Inspect OrdersPage and every component used inside it:
   - OrderCard
   - OrderStatusTimeline
   - OrderDetailsModal
   - Receipt/Ticket buttons
   - filter chips
   - payment/status badges

2. Search inside these files for:
   t(
   tStatus(
   tPaymentMethod(
   tRole(
   tAction(

3. For every usage, ensure:
   const { t, tStatus, tPaymentMethod, tRole, tAction } = useT();

4. Fix missing imports:
   import { useT } from "../../i18n/useT";
   or correct relative path.

5. Add safe fallback around OrdersPage:
   - never render black screen
   - if orders fetch fails, show visible error card
   - if translation fails, fallback to plain text

6. Check console error after build.
   Specifically remove:
   ReferenceError: t is not defined

7. Do not change backend.
8. Do not change socket logic.
9. Do not remove features.

Run:
npm run build

Commit and push:
git add .
git commit -m "Fix OrdersPage black screen"
git push origin main

Acceptance:
- /admin/orders opens normally
- no black screen
- no ReferenceError t is not defined
- orders list or empty state is visible
- filters visible
- page remains responsive