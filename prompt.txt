URGENT FIX customer QR routing and bottom navigation.

Current problems:
1. When scanning QR, it sometimes opens menu directly instead of welcome/home page.
2. On customer welcome page, clicking cart icon sends user to welcome/home instead of cart.
3. Bottom navigation routes are wrong/confused.

Correct routing:

QR scan URL:
 /r/:slug/table/:tableId

This route MUST always render TableWelcome / customer home page.

Customer welcome page:
- “Voir le menu” button => /r/:slug/table/:tableId/menu
- cart icon => /r/:slug/table/:tableId/cart
- assistance icon/button => /r/:slug/table/:tableId/assistance

Customer menu page:
- cart icon => /r/:slug/table/:tableId/cart
- bottom nav Menu => /r/:slug/table/:tableId/menu
- bottom nav Cart => /r/:slug/table/:tableId/cart
- bottom nav Assistance => /r/:slug/table/:tableId/assistance

Customer cart page:
- bottom nav Menu => menu
- Cart => cart
- Assistance => assistance

Do NOT redirect automatically from welcome to menu.
Remove any useEffect or navigate() that sends /r/:slug/table/:tableId directly to menu.

Important:
- Keep tableId preserved everywhere.
- Keep slug preserved everywhere.
- No NaN tableId.
- Cart route must never point to welcome.
- Menu route must never replace welcome route.

Files to inspect:
- router/index.jsx
- App.jsx
- TableWelcome.jsx
- MenuPage.jsx
- CustomerLayout.jsx
- BottomNav.jsx
- CartPage.jsx
- CheckoutPage.jsx

Acceptance:
- scanning QR opens welcome page
- clicking Voir le menu opens menu
- clicking cart opens cart
- bottom nav works correctly
- no automatic redirect to menu
- build passes

Run:
npm run build
git add .
git commit -m "Fix customer QR routing and bottom navigation"
git push origin main