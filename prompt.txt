Fix order creation NaN integer error.

Current backend error:
invalid input syntax for type integer: "NaN"

The checkout page crashes when confirming an order.

Tasks:

1. Inspect frontend order payload before POST /api/orders
Add temporary console.log(payload)

2. Validate all integer fields:
- table_id
- restaurant_id
- quantity
- product_id

3. Convert safely:
Number(value)

4. Never send NaN values.
If invalid:
- show toast error
- stop submit

5. Backend validation:
Before SQL insert:
- reject NaN
- reject undefined
- reject null integers

6. Fix QR/table extraction logic.
Likely issue:
tableId parsed from URL incorrectly.

7. Add safe fallback:
const tableId = Number(params.tableId || 1)

8. Build, verify order creation works, commit, push.

Acceptance:
- Order confirms successfully
- No PostgreSQL NaN error
- Orders appear in admin
- Mobile checkout works