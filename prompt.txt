Fix the mobile responsive layout of the "Server Calls" page in BarOrder QR.

IMPORTANT:
- Directly modify existing project files.
- Do not explain.
- Do not output code examples only.
- Apply the fixes in the actual workspace files.
- Keep the current design system and colors.

PROBLEMS TO FIX:

1. Status badge overlaps table text
- "En attente" overlaps "Table 1"
- Add proper spacing and responsive wrapping.

2. Mobile card layout is too compressed
- Current horizontal layout breaks on iPhone screens.
- On mobile (<640px):
  - stack content vertically
  - icon on top/left
  - table + status below
  - action buttons below content

3. Buttons overflow
- "Accepter" and "Résoudre" must:
  - wrap correctly
  - stay inside card
  - become full width on very small screens

4. Improve spacing
- Add proper gaps between:
  - icon
  - table text
  - status badge
  - datetime
  - buttons

5. Typography
- Prevent text collision.
- Prevent badge overflow.
- Use truncate/wrap correctly.

6. Responsive structure
Desktop:
- horizontal layout

Mobile:
- vertical stacked layout

7. Add safe responsive utilities
- min-width:0
- flex-wrap
- overflow-hidden
- break-words
- gap utilities
- responsive padding

8. Verify:
- no overlap
- no horizontal scroll
- buttons fully visible
- cards readable on iPhone SE and iPhone 12/13

9. Build project after modifications
Run:
npm run build

Fix all responsive errors automatically.