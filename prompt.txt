Fix production API route mismatch in BarOrder QR.

Current Render logs show:
POST /public/orders 404
GET /public/r/le-palais/menu 404
GET /api/restaurants 404

But backend working route is:
GET /api/public/menu/1

Required fixes:

1. In client/src, search all API calls using:
   /public/orders
   /public/r/
   /api/restaurants
   /restaurants

2. All backend calls must use API base:
   const API_URL = import.meta.env.VITE_API_URL || "http://localhost:5000"

3. Fix customer order creation:
   POST must call:
   `${API_URL}/api/public/orders`

4. Fix menu by slug:
   GET must call:
   `${API_URL}/api/public/r/${slug}/menu/${tableId}`
   or match the exact backend existing route.

5. Fix restaurant list:
   If backend route /api/restaurants does not exist, add it in Express OR change frontend to use the existing backend route.
   Preferred: add backend route:
   GET /api/restaurants
   returning all restaurants with id, name, slug, address, phone.

6. Do not use:
   /public/orders
   /public/r/...
   without /api.

7. Remove mock fallback in production.
   If API fails, show real error.

8. Run:
   npm run build

9. Do not change UI design.