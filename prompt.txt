Fix dashboard daily revenue statistics.

Current issue:
“Chiffre d’affaires du jour” is incorrect or not updating properly.

Implement REAL revenue calculations.

Requirements:

1. Daily revenue must calculate:
SUM(total_amount)
ONLY for:
- orders with status = paid OR served
- current day only
- current restaurant only

2. Exclude:
- cancelled orders
- pending carts
- test/demo orders
- deleted orders

3. Revenue updates instantly:
- when payment validated
- when order marked served
- without page refresh if websocket/realtime exists

4. Add proper metrics:
- todayRevenue
- totalOrdersToday
- averageBasket
- topSellingItems

5. Add fallback:
if no orders today:
display:
0 FCFA
and “Aucune vente aujourd’hui”

6. Use backend calculation.
Do NOT hardcode frontend numbers.

7. Add proper timezone handling:
Africa/Dakar

8. Optimize:
- avoid recalculating every render
- use aggregation query

9. Mobile responsive dashboard cards.

10. Do not break existing dashboard pages.

Build and push.