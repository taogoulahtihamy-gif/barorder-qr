Fix kitchen status movement and annoying print ticket page.

PROBLEM 1 — Kitchen status does not move visually:
On /admin/kitchen, when clicking “Commencer” or “Prête”, the toast says “Statut mis à jour” but the order card stays in the same column until reload.

Fix:
1. After successful status update, update local state immediately.
2. Move the order card to the correct column without refresh:
   - Nouvelle -> En préparation after Commencer
   - En préparation -> Prête after Prête
3. Also refetch orders after update as fallback.
4. Ensure status names are normalized:
   - nouvelle/pending -> nouvelle
   - accepted -> accepted
   - preparing/en_preparation -> preparing
   - ready/prete -> ready
5. Kitchen columns must use backend status consistently.
6. No duplicate order cards.

PROBLEM 2 — Print button opens annoying ticket page/tab:
Currently, clicking print opens a separate ticket page that stays open.

Fix:
1. Do not navigate away from admin page.
2. Use hidden printable iframe or window that:
   - opens print dialog
   - prints ticket
   - closes automatically after print
3. For browser fallback, open a small print window and auto-close after 1 second.
4. Kitchen print should print only kitchen ticket, not full invoice page.
5. Admin order print should not create many tabs.

DESIGN:
Keep current premium kitchen design.

TESTS:
- Click Commencer: card moves to En préparation immediately.
- Click Prête: card moves to Prête immediately.
- Print ticket: print dialog opens, no annoying persistent page.
- No reload needed.
- npm run build passes.
- commit and push.