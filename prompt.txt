EMERGENCY PRODUCTION FIX — FIND AND FIX THE REAL DEPLOYED FILE.

Problem:
Render still crashes with:

src/routes/users.js imports updateUser/deleteUser
but ../controllers/userController.js does not export updateUser/deleteUser.

This means the real deployed file still contains broken imports.

DO NOT explain.
DO NOT give code examples.
DIRECTLY edit the real project files.

Tasks:

1. Search the entire repository for:
updateUser
deleteUser
userController

2. Find every file importing updateUser/deleteUser from userController.

3. Fix src/routes/users.js and any duplicate users route file.

Replace broken imports with only existing exports:
getUsers
createUser

4. Remove/comment every route using:
updateUser
deleteUser

5. If user management is incomplete, disable the /api/admin/users edit/delete routes temporarily.

6. Keep only safe routes:
GET /api/admin/users
POST /api/admin/users

7. Do not touch working modules:
- public restaurants
- menu
- orders
- payments
- kitchen
- server calls
- admin dashboard

8. Ensure login uses:
password_hash
not password

9. Ensure admin@barorder.sn / admin123 works.

10. Run backend locally:
npm start

11. Run build if needed.

12. Commit and push to main.

Acceptance test:
- Search result shows no import of updateUser/deleteUser.
- Backend starts without crash.
- Render deploy succeeds.
- /api/public/restaurants returns 200.
- /api/public/r/le-palais/menu returns 200.
- Admin login works with admin@barorder.sn / admin123.