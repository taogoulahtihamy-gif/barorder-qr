Fix production order status error.

Real error:
column "accepted_at" of relation "orders" does not exist

Cause:
The order workflow code uses timestamp columns that are missing in the Neon orders table.

Directly modify backend migrations/startup schema.

Tasks:

1. Ensure the orders table has these columns:
accepted_at TIMESTAMP NULL
preparing_at TIMESTAMP NULL
ready_at TIMESTAMP NULL
served_at TIMESTAMP NULL
paid_at TIMESTAMP NULL
cancelled_at TIMESTAMP NULL

2. Add safe migration using:
ALTER TABLE orders ADD COLUMN IF NOT EXISTS ...

3. Do not drop data.
4. Do not recreate the orders table.
5. Do not break existing orders.

6. Check every status update:
- pending/new/nouvelle => no timestamp required
- accepted => accepted_at = NOW()
- preparing => preparing_at = NOW()
- ready => ready_at = NOW()
- served => served_at = NOW()
- paid => paid_at = NOW()
- cancelled => cancelled_at = NOW()

7. If the app uses French statuses, map them safely to backend statuses.

8. Backend must not crash if old orders have null timestamps.

9. Run:
npm run build
node --check src/server.js

10. Commit and push.

Acceptance:
- Clicking “Accepter” no longer causes accepted_at error.
- Order status changes from Nouvelle to Acceptée.
- Kitchen workflow still works.
- No data lost.