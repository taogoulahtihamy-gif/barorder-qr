The current deployment is broken because of the multi-role admin system.

DO NOT remove the project.
DO NOT rollback the entire application.
DO NOT change the UI/design.

Fix the authentication and role system properly and make the deployment stable.

IMPORTANT:
The Render crash comes from:
src/routes/users.js importing:
updateUser
deleteUser
from userController.js while those exports do not exist.

Tasks:

1. Fix ALL backend import/export mismatches.
- Ensure every imported controller function actually exists.
- If updateUser/deleteUser are unused, remove their imports and routes.
- If they are needed, create proper exported functions.

2. Keep ONLY this stable auth structure:
- admin@barorder.sn
- super@barorder.sn
- manager@barorder.sn
- waiter@barorder.sn
- kitchen@barorder.sn
- cashier@barorder.sn

password for all:
admin123

3. Make login work with Neon PostgreSQL users table.
The table columns are:
id
restaurant_id
name
email
password_hash
role
created_at
is_active

VERY IMPORTANT:
Use password_hash instead of password everywhere.

4. Fix bcrypt comparison correctly:
bcrypt.compare(password, user.password_hash)

5. Make JWT auth stable again.

6. Make role middleware stable:
- super_admin => full access
- admin => admin access
- manager => dashboard/orders/payments
- waiter => orders + calls
- kitchen => kitchen only
- cashier => payments only

7. NEVER crash the server if a role route is missing.
Gracefully disable unavailable routes.

8. Ensure:
npm install
npm run build
npm start
all pass without errors.

9. Remove every broken import in:
routes/users.js
routes/auth.js
controllers/userController.js

10. Verify deployment readiness:
- no syntax errors
- no missing exports
- no undefined middleware
- no crashing routes

11. Commit all fixes directly to the project files.
12. Push to GitHub.
13. Ensure Render can deploy successfully after the fix.

DO NOT explain.
DIRECTLY MODIFY FILES.