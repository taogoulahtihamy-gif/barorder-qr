Improve admin login and user password fields without breaking anything.

Scope:
- admin login page only
- admin users page password fields only
- do not touch backend logic
- do not touch roles/permissions
- do not touch orders, kitchen, payments, QR, menu, checkout

Tasks:

1. Admin login page
Improve /admin/login into a cleaner premium version:
- dark luxury BarOrder style
- centered login card
- better spacing
- better typography
- gold/emerald accents
- mobile responsive
- no horizontal overflow
- no demo credentials displayed
- no text already written inside the email input

Important:
- Email input must be empty by default.
- Password input must be empty by default.
- Do not prefill admin@barorder.sn or any demo email.
- Keep placeholders only:
  FR: "Adresse email"
  EN: "Email address"
  FR: "Mot de passe"
  EN: "Password"

2. Password visibility icon on login
Add an eye icon inside the password field:
- click eye => show password
- click again => hide password
- icon changes between Eye and EyeOff if lucide-react is available
- button must be accessible with aria-label
- must not submit the form accidentally
- must work on mobile and desktop

3. Users page password fields
On /admin/users:
Add eye icon only to password fields:
- create user password field
- reset password field
- edit password field if it exists

Rules:
- Do NOT add eye icon to email fields.
- Do NOT add eye icon to name/role fields.
- Only password inputs.

4. Security/UX
- Default password state remains hidden.
- Showing password only changes the input type locally.
- Do not store visible password anywhere.
- Do not log password in console.
- Do not change API payload structure.

5. Bilingual
All new labels/tooltips must use existing i18n system:
FR:
- Afficher le mot de passe
- Masquer le mot de passe
EN:
- Show password
- Hide password

6. Technical
Use existing icon library if present:
lucide-react Eye / EyeOff
If not available, use simple text/icon fallback.

7. Build and push
Run:
npm run build

Then:
git add .
git commit -m "Improve admin login and password visibility controls"
git push origin main

Acceptance:
- /admin/login looks more premium
- email field is empty by default
- password field has working eye icon
- /admin/users password fields have working eye icon
- no backend broken
- no role system broken
- build passes